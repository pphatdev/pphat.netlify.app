"use client";
// import { createDB } from "@lib/db/db";

export const TestDb = () => {

    // createDB("test", { timestamps: true, version: 1, type: "indexedDB" })

    // console.log(createDB("test", { timestamps: true, version: 1, type: "indexedDB" }));

    return <></>
}